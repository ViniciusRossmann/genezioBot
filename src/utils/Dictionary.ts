export const dict = {
    hotword: [ "jamal" ],
    play: [
        "play", "toque", "tocar", "toca"
    ],
    skip: [
        "skip", "pule", "pular", "pula", "polar"
    ]
}

export function contains(speech_content: string, words: string[]) {
    
}